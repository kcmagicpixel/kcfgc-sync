{
  "name": "@emily-curry/fgc-sync-root",
  "version": "0.0.0",
  "private": true,
  "author": "Emily Curry <emily@curry.sh>",
  "license": "MIT",
  "packageManager": "pnpm@8.3.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "watch": "pnpm -r --parallel --stream run watch",
    "format": "pnpm -r --parallel run format"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^5.0.4"
  }
}
